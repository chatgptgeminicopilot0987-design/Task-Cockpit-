{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "node -e \"const {execSync}=require('child_process');execSync('pnpm --filter @workspace/team-task-manager run build',{stdio:'inherit'});execSync('pnpm --filter @workspace/api-server run build',{stdio:'inherit'});\"",
    "start": "node --enable-source-maps ./artifacts/api-server/dist/index.mjs",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
